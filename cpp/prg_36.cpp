#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    int a, c, d, e, f, g;
    float b;
    cout << "\n enter a value :";
    cin >> a;
    cout << "\n sqrt value is :" << sqrt(a);
    cout << "\n abs value is :" << abs(a);
    cout << "\n pow value is :" << pow(a, 2);
    cout << "\n fabs value is :" << fabs(a);
    cout << "\n floor value is :" << floor(a);
    cout << "\n ceil value is :" << ceil(a);
}